# Social Media Batch Publisher - 用户指南

> 从安装到发布的完整指南

---

## 📚 目录

1. [快速开始](#快速开始)
2. [安装教程](#安装教程)
3. [配置教程](#配置教程)
4. [使用指南](#使用指南)
5. [常见问题](#常见问题)

---

## 🚀 快速开始

### 三步发布视频

1. **准备内容** - 填Excel，放视频
2. **配置工具** - 填写API凭证
3. **运行发布** - 一条命令搞定

**时间**: 首次配置10-30分钟，之后每次3分钟

---

## 📦 安装教程

### 方式一：AI辅助安装（推荐 - 10分钟）

**前提**: 已安装Claude Code或类似AI CLI工具

**步骤**:

```bash
# 1. 下载项目
git clone https://github.com/xisheng687/social-media-batch-publisher.git
cd social-media-batch-publisher

# 2. 打开Claude Code
code .  # 或在VS Code中打开此文件夹
```

**3. 对AI说**:

```
你好！请帮我完成以下操作：

1. 阅读README.md和用户指南.md，了解这个项目
2. 运行 npm install 安装依赖
3. 我会提供Metricool和R2凭证，请帮我生成config.js
4. 指导我如何使用这个工具

开始吧！
```

**4. 跟随AI指引完成配置**

AI会自动：
- 安装依赖
- 生成配置文件
- 解答你的问题
- 帮你排查错误

---

### 方式二：简单手动安装（15分钟）

**前提**: 已安装Node.js (≥14.0.0)

```bash
# 1. 下载项目
git clone https://github.com/xisheng687/social-media-batch-publisher.git
cd social-media-batch-publisher

# 2. 安装依赖
npm install

# 3. 复制配置文件
cp config.example.js config.js

# 4. 编辑config.js，填入你的凭证
# 用文本编辑器打开config.js，填入API凭证
```

**如何获取凭证？** → 查看[配置教程](#配置教程)

---

### 方式三：详细手动安装（30分钟）

**适合**: 想完全理解每一步的用户

#### 步骤1：安装Node.js

1. 访问 [nodejs.org](https://nodejs.org)
2. 下载LTS版本（推荐18.x或20.x）
3. 运行安装程序
4. 验证安装：
   ```bash
   node --version  # 应显示 v18.x.x 或更高
   npm --version   # 应显示 9.x.x 或更高
   ```

#### 步骤2：下载项目

**选项A：使用Git**
```bash
git clone https://github.com/xisheng687/social-media-batch-publisher.git
cd social-media-batch-publisher
```

**选项B：直接下载**
1. 访问GitHub仓库页面
2. 点击 "Code" → "Download ZIP"
3. 解压到任意文件夹
4. 打开命令行，进入该文件夹

#### 步骤3：安装依赖

```bash
npm install
```

这会下载所有需要的库（如axios、xlsx等）到`node_modules/`文件夹。

**说明**:
- `node_modules/` 包含几千个文件，这是正常的
- 包含googleapis、google-auth-library等，这是运行需要的依赖
- 这个文件夹已在`.gitignore`中，不会上传到GitHub

#### 步骤4：创建配置文件

```bash
cp config.example.js config.js
```

用任何文本编辑器打开`config.js`，按[配置教程](#配置教程)填入凭证。

---

## ⚙️ 配置教程

### 你需要准备的凭证

| 服务 | 需要获取的凭证 | 时间 |
|------|---------------|------|
| Metricool | Token, User ID, Blog ID | 5分钟 |
| Cloudflare R2 | Access Key, Secret Key, Endpoint, Bucket, Public URL | 10-15分钟 |

---

### 一、Metricool配置（5分钟）

#### 1. 获取Metricool凭证

**前提**: 已有Metricool付费账号

1. 登录 [Metricool](https://metricool.com)
2. 打开浏览器开发者工具：
   - Chrome/Edge: `F12` 或 `Ctrl+Shift+I`
   - Safari: `Cmd+Option+I`
3. 切换到 **Network** (网络) 标签
4. 刷新页面
5. 在请求列表中找到任何API请求
6. 查看请求头 (Request Headers)，找到：
   - `Authorization: Bearer xxxxx` → 这是你的 **METRICOOL_TOKEN**
   - URL参数中的 `userId=xxx` → 这是你的 **METRICOOL_USER_ID**
   - URL参数中的 `blogId=xxx` → 这是你的 **METRICOOL_BLOG_ID**

#### 2. 填入config.js

```javascript
METRICOOL_TOKEN: "你复制的Token（很长的字符串）",
METRICOOL_USER_ID: "你的User ID（数字）",
METRICOOL_BLOG_ID: "你的Blog ID（数字）",
```

---

### 二、Cloudflare R2配置（15分钟）

⚠️ **重要**: Cloudflare界面经常更新，以下步骤中的按钮名称仅供参考，**请以实际界面为准**。

#### 第1步：创建Cloudflare账号

访问 [cloudflare.com](https://cloudflare.com) 注册免费账号

---

#### 第2步：创建R2存储桶

1. 登录Cloudflare Dashboard
2. 左侧菜单选择 **"R2"**
3. 点击 **"Create bucket"** 按钮
4. 输入名称：`metricool-videos`（或其他你喜欢的名字）
5. 区域：选默认即可
6. 点击 **"Create bucket"** 确认

✅ 记下这个名字，这是你的 `R2_BUCKET_NAME`

---

#### 第3步：启用公开访问

⚠️ **注意**: 按钮名称可能是 "Allow Access"、"Enable Public Access"、"Connect Domain" 等，以实际界面为准

1. 进入刚创建的bucket
2. 找到 **Settings** 标签（或 "设置"）
3. 找到 **Public Access**（公开访问）相关部分
4. 启用公开访问（可能是开关按钮）

✅ 启用后会生成一个 **Public URL**，类似：
```
https://pub-xxxxxxxxxxxxxx.r2.dev
```

这是你的 `R2_PUBLIC_URL`，复制保存

---

#### 第4步：获取Endpoint地址

在bucket页面找到 **S3 API** 或 **Endpoint** 信息

格式：
```
https://[你的账号ID].r2.cloudflarestorage.com
```

**如何找到账号ID？**
- 查看浏览器地址栏：`https://dash.cloudflare.com/[这串就是账号ID]/r2/buckets`
- 账号ID是32位16进制字符串（如：`a1b2c3d4e5f6...`）

**示例**：
```
https://1234567890abcdef1234567890abcdef.r2.cloudflarestorage.com
```

这是你的 `R2_ENDPOINT`

---

#### 第5步：创建API Token

1. 点击 **"Manage R2 API Tokens"** 或类似按钮
2. 点击 **"Create API Token"**
3. 权限设置：
   - **Token name**: `metricool-api`（随便起名）
   - **Permissions**: 选择 **"Admin Read & Write"**（管理读写权限）
   - 或更安全：只勾选 **Object Read & Write**（对象读写）
4. 点击 **"Create API Token"**

**重要**：创建后会显示：
- **Access Key ID** → 这是你的 `R2_ACCESS_KEY_ID`
- **Secret Access Key** → 这是你的 `R2_SECRET_ACCESS_KEY`

⚠️ **Secret Access Key只显示一次，务必保存！** 如果忘记，需要重新创建Token。

---

#### 第6步：设置自动删除规则（重要！）

**目的**: 7天后自动删除视频，节省成本

⚠️ **注意**: 界面元素名称可能略有不同，如 "Lifecycle rules"、"Lifecycle policies" 等

1. 在bucket页面找到 **Settings** 标签
2. 找到 **Lifecycle**（生命周期）相关部分
   - 可能叫 "Object lifecycle rules"
   - 或 "Lifecycle policies"
3. 点击 **"Add Rule"** 或 "Create rule"
4. 配置规则：
   - 规则名称: 自定义（如 `auto-delete-7days`）
   - 操作类型: **Delete**（删除）
   - 天数: **7**（7天后删除）
5. 点击保存

✅ 设置完成！7天后视频会自动删除

**成本计算**：
```
20个视频 × 0.35MB × 7天 ≈ 0.05GB
成本: $0.015/GB × 0.05GB ≈ $0.0008 (不到0.1分钱)
```

---

#### 第7步：填入config.js

打开`config.js`，填入所有R2凭证：

```javascript
module.exports = {
    // Metricool凭证
    METRICOOL_TOKEN: "你的Metricool Token",
    METRICOOL_USER_ID: "你的User ID",
    METRICOOL_BLOG_ID: "你的Blog ID",

    // Cloudflare R2凭证
    R2_ACCESS_KEY_ID: "你的Access Key ID",
    R2_SECRET_ACCESS_KEY: "你的Secret Access Key",
    R2_ENDPOINT: "https://你的账号ID.r2.cloudflarestorage.com",
    R2_BUCKET_NAME: "metricool-videos",
    R2_PUBLIC_URL: "https://pub-xxxxx.r2.dev",

    // 视频设置
    VIDEOS_FOLDER: "./videos",

    // 代理设置（可选）
    USE_PROXY: true,   // 如果需要代理访问Metricool
    PROXY_HOST: "127.0.0.1",
    PROXY_PORT: 7897   // Clash默认端口
};
```

✅ 配置完成！

---

## 📖 使用指南

### 核心原理：文件名匹配

⚠️ **重要**: 系统通过 **Excel标题 = 视频文件名** 来匹配

```
Excel中的"标题"列  ⟷  视频文件名（不含扩展名）
```

**示例**：

```
✅ 正确:
Excel标题: My First Video
视频文件: My First Video.mp4

❌ 错误:
Excel标题: My First Video
视频文件: video_001.mp4  ← 不匹配，系统找不到！
```

---

### 第1步：准备Excel表格

1. 打开 `贴文模板.xlsx`
2. 填写三列：

| 列名 | 说明 | 示例 |
|------|------|------|
| 标题 | 视频主题（**必须**与文件名一致） | My First Video |
| 状态 | 填 `准备好` | 准备好 |
| 贴文内容 | 发布的文案 | Check out my new video! #socialmedia |

**多条内容**：每行一条，一次可以准备20-30条

**示例**：
```
行1: 标题=My First Video,  状态=准备好, 内容=xxx
行2: 标题=My Second Video, 状态=准备好, 内容=xxx
行3: 标题=My Third Video,  状态=准备好, 内容=xxx
...
```

---

### 第2步：准备视频文件

1. 将视频文件放入 `videos/` 文件夹
2. **文件名必须与Excel标题完全一致**

**文件要求**：
- 格式: `.mp4` 或 `.mov`
- 时长: 建议 ≤60秒
- 大小: <100MB
- 命名: 与Excel标题一致

**示例**：
```
videos/
├── My First Video.mp4   ← 对应Excel第1行
├── My Second Video.mp4  ← 对应Excel第2行
└── My Third Video.mp4   ← 对应Excel第3行
```

---

### 第3步：运行发布

```bash
node src/process_excel_posts.js
```

**系统会自动**：
1. 读取Excel中状态为"准备好"的行
2. 根据标题匹配视频文件
3. 上传视频到Cloudflare R2
4. 获取公开链接
5. 创建Metricool排期（默认每天晚上8点发一条）
6. 更新Excel状态为"已发布"
7. 在Excel中添加：
   - 视频url（R2链接）
   - 发布时间
   - 处理时间

**发布成功后**：
- Excel会自动更新
- 视频已排期，到时间自动发布
- 可以在Metricool网页查看排期

---

### 第4步：日常使用

**用AI（推荐）**：

```
对Claude Code说：
"帮我把这20个视频发布到Instagram和TikTok，
从明天开始每天晚上8点发一条"

AI会：
- 读取Excel
- 匹配视频
- 执行发布
- 报告结果
```

**手动执行**：

```bash
# 批量发布
node src/process_excel_posts.js

# 删除所有排期
node src/delete_all_posts.js

# 重置Excel状态（重新排期前）
node src/reset_excel_status.js
```

---

## ❓ 常见问题

### Q: 为什么我的视频找不到？

**A**: 检查文件名是否与Excel标题完全一致

```bash
# 检查文件名
ls videos/

# 对比Excel中的标题
# 包括：
- 大小写
- 空格
- 标点符号
- 特殊字符
```

---

### Q: 如何修改发布时间？

**A**: 默认每天晚上8点。

**用AI修改**（推荐）：
```
对AI说："帮我修改发布时间为每天早上9点"
AI会帮你修改src/process_excel_posts.js中的时间设置
```

**手动修改**：
编辑 `src/process_excel_posts.js`，找到：
```javascript
scheduledTime.setHours(20, 0, 0, 0);  // 20改为其他小时
```

---

### Q: TikTok连接过期怎么办？

**A**:
1. 打开Metricool网页
2. Settings → Social Networks
3. 断开TikTok连接
4. 重新授权连接

---

### Q: Excel可以修改吗？

**A**: 可以！但请保留这三列：
- 标题
- 状态
- 贴文内容

其他列可以删除，系统会自动添加需要的列。

---

### Q: 如何重新排期？

**A**:
```bash
# 1. 删除所有已排期的帖子
node src/delete_all_posts.js

# 2. 重置Excel状态
node src/reset_excel_status.js

# 3. 重新运行发布
node src/process_excel_posts.js
```

---

### Q: 视频7天后会删除吗？

**A**: 是的，如果你按教程设置了R2的Lifecycle规则。

- R2上的视频会自动删除
- **重要视频请本地备份**
- Metricool上的排期不受影响（视频已发布到各平台）

---

### Q: 为什么node_modules有这么多文件？

**A**: 这是正常的npm依赖包文件夹。

- 包含axios、xlsx、googleapis等运行需要的库
- 已在`.gitignore`中，不会上传GitHub
- 不用担心，这是JavaScript项目的标准结构

---

### Q: videos/.gitkeep是什么？

**A**: 这是一个占位文件，让git跟踪空文件夹。

- 作用：保持videos文件夹在git中被跟踪
- 可以忽略这个文件
- 当你放入视频后，这个文件就无关紧要了

---

### Q: 成本大概多少？

**A**:

**Metricool**: $15-30/月（必需）
**Cloudflare R2**: ~$0.01/月（20个视频）

**总计**: $15-30/月

---

## 🆘 需要帮助？

1. **查看技术文档** - `docs/Metricool API文档.md` 和 `docs/技术说明.md`
2. **使用AI** - 对Claude Code描述问题，它会帮你解决
3. **提Issue** - 在GitHub仓库提交问题

---

**祝你使用愉快！** 🎉

*如有问题，欢迎反馈*
